import { MantineTheme } from '@mantine/core'

export const theme = {
    globalStyles: (theme?: MantineTheme) => ({
        img: {
            margin: 0,
        },
        '.mantine-Text-root': {
            color: '#000',
        },
    }),
}
